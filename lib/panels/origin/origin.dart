import 'package:flutter/material.dart';

class OriginPanel extends StatelessWidget {
  const OriginPanel({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Coming"),
    );
  }
}
